# Single-project Vercel deployment: the API is served on the same origin under
# /api, so this must stay empty. Only set it if you deploy the backend to a
# separate domain (e.g. Railway/Render) instead of Vercel serverless functions.
NEXT_PUBLIC_API_URL=
